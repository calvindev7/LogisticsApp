@model List<LogisticsApp.Models.Cliente>

<h3>Listado de Clientes</h3>

@if (Model == null)
{
    <p>Cargando...</p>
}
else if (Model.Count == 0)
{
    <p>No hay clientes disponibles.</p>
}
else
{
    <div class="row">
        <div class="col-md-6">
            <a asp-action="Create" class="btn btn-info" data-bs-toggle="tooltip" data-bs-placement="top" title="Crear Cliente"><i class="bi bi-plus-circle"></i> Crear Cliente</a>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th class="text-center">Nombre</th>
                    <th class="text-center">Email</th>
                    <th class="text-center">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var cliente in Model)
                {
                    <tr>
                        <td class="text-center">@cliente.Nombre</td>
                        <td class="text-center">@cliente.Email</td>
                        <td class="text-center">
                            <a asp-action="Edit" asp-route-id="@cliente.Id" class="btn btn-info" data-bs-toggle="tooltip" data-bs-placement="top" title="Editar"><i class="bi bi-pencil"></i></a>
                            <a asp-action="Details" asp-route-id="@cliente.Id" class="btn btn-primary" data-bs-toggle="tooltip" data-bs-placement="top" title="Detalles"><i class="bi bi-info-circle"></i></a>
                            <a asp-action="Delete" asp-route-id="@cliente.Id" class="btn btn-danger" data-bs-toggle="tooltip" data-bs-placement="top" title="Eliminar"><i class="bi bi-trash"></i></a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>

    @section Scripts {
    @{
            await Html.RenderPartialAsync("_ValidationScriptsPartial");

            <script>
                var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
                var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                    return new bootstrap.Tooltip(tooltipTriggerEl);
                });
            </script>
        }
    }
}